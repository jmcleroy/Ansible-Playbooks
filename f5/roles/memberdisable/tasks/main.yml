---
- name: Force node offline
  bigip_node:
      server: "192.168.1.140"
      state: "present"
      user: "{{ user }}"
      password: "{{ password }}"
      session_state: "disabled"
      monitor_state: "disabled"
      partition: "Common"
      host: "{{ host }}"
