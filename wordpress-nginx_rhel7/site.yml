---
- name: Install WordPress, MariaDB, Nginx, and PHP-FPM
  hosts: all
  remote_user: root
  # remote_user: user
  # sudo: yes

  roles:
    - subscribe
    - common
    - mariadb
    - nginx
    - php-fpm
    - wordpress
